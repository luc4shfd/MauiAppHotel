<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MauiAppHotel.Views.SobreDesenvolvedor"
             Title="SobreDesenvolvedor" NavigationPage.HasNavigationBar="False"
                                        BackgroundColor="{StaticResource Gray100}">


    <VerticalStackLayout VerticalOptions="CenterAndExpand"
                         Padding="10"
                         Spacing="10">


        <Label 
            Text="Sobre o Desenvolvedor"
            FontSize="30"
            VerticalOptions="Center" 
            HorizontalOptions="Center"
            TextColor="{StaticResource quarta}"/>



        <Label 
            Text="Desenvolvido por lucas"
            FontSize="19"
            VerticalOptions="Center" 
            HorizontalOptions="Center"
            TextColor="{StaticResource quinta}"/>

        <Label 
            Text="Técnico Desenvolvimento de Sistemas"
            FontSize="19"
            VerticalOptions="Center" 
            HorizontalOptions="Center"
            TextColor="{StaticResource quinta}"/>

        <Button Text="Contato" Clicked="Button_Clicked" />

    </VerticalStackLayout>
</ContentPage>